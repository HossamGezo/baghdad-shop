{
  "name": "baghdad-shop-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root management for Baghdad Shop (Client & Server)",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"magenta,blue\" \"npm run server --prefix server\" \"npm run dev --prefix client\"",
    "install-all": "npm install --prefix client && npm install --prefix server",
    "client-only": "npm run dev --prefix client",
    "server-only": "npm run server --prefix server",
    "prepare": "husky",
    "format": "prettier --write .",
    "tree": "tree -I 'node_modules|dist'"
  },
  "lint-staged": {
    "client/**/*.{ts,tsx,css}": [
      "prettier --write"
    ],
    "server/**/*.{ts,js,json}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.7.2"
  }
}
