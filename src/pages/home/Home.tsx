// --- Local Components
import HeroSection from "./hero-section/HeroSection";

// --- Home (Main Component)
const Home = () => {
  // --- Return JSX
  return <HeroSection />;
};

export default Home;
